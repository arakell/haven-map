- map generation buffers, then only paint them onto the map
- store tiles (with paths and date) in a db and read only those from disk
- initial view offset
- initial tile offset (defaults to view offset)
- cave layer(s)
